<%= simple_form_for(@budget_category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: false, placeholder: 'New Budget Category' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
