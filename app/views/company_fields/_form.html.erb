<%= simple_form_for(@company_field) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :company %>
    <%= f.association :field %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
