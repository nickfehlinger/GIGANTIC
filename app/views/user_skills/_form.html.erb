<%= simple_form_for(@user_skill) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :skill %>
    <%= f.association :UserField %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
